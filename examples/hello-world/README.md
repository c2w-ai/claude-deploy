# hello-world

Tiny zero-dependency Node HTTP server used as a smoke test for the `/deploy` plugin.

## Run locally

```bash
node server.js
# open http://localhost:3000
```

## Deploy

From inside Claude Code with the `claude-deploy` plugin installed:

```
/deploy hello-world
```
