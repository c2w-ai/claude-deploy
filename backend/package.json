{
  "name": "claude-deploy-backend",
  "version": "0.1.0",
  "description": "Receives tarballs from the claude-deploy Claude Code plugin and deploys them to Railway.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {}
}
