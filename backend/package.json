{
  "name": "claude-deploy-backend",
  "version": "0.1.0",
  "description": "Hosted backend for the claude-deploy Claude Code plugin. Receives gzipped tarballs, authenticates users via Supabase, builds + serves them on a public URL.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {}
}
