node_modules/
*.log
.DS_Store
.env
.env.*
!.env.example
dist/
build/
.vscode/
.idea/
tmp/
*.tar.gz
# Workspace / per-machine Claude Code config — MCP server definitions may
# contain personal access tokens (e.g. Supabase, GitHub). Never commit.
.mcp.json
.claude/
# Planning artifacts (Claude Code plan mode writes here)
.plans/
